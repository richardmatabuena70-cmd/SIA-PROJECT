MATH QUIZ GAME - DASHBOARD & QUIZ INTERFACE IMPLEMENTATION PROMPT
===================================================================

Use this prompt to recreate the same dashboard, hidden sidebar, centered quiz interface, and smooth box buttons:

---

DASHBOARD REQUIREMENTS:
-----------------------
1. Hidden/Collapsible Sidebar:
   - Sidebar starts collapsed (hidden off-screen)
   - Click on "Dashboard" header to toggle sidebar visibility
   - Smooth CSS transition: transform: translateX(-220px) when collapsed
   - Sidebar contains buttons: Play Game, Current Score, Remaining Time, Accuracy, Response Review, Question Progress, User Account, Rank Score, Logout

2. Theme Toggle:
   - Dark/Light mode toggle switch (top right corner)
   - Smooth transition between themes
   - Toggle shows ☀️/🌙 icons

3. Quiz Interface (centered):
   - When quiz mode is active, content is centered with flexbox
   - All quiz elements (questions, choices) are centered
   - Use CSS class "quiz-mode" on #content element
   - Apply: display: flex; flex-direction: column; align-items: center; justify-content: flex-start; text-align: center;

4. Smooth Box Buttons:
   - All buttons should have smooth transitions
   - Border-radius for rounded corners
   - Hover effects with subtle shadows
   - Use CSS transition: all 0.3s ease;

---

FILE STRUCTURE:
---------------
frontend/
├── dashboard.html          (main dashboard with sidebar)
├── css/
│   └── dashboard.css       (all styling for dashboard, sidebar, quiz mode)
├── js/
│   ├── dashboard.js        (sidebar toggle, theme toggle, dashboard functions)
│   └── quiz.js             (quiz logic and rendering)
└── index.html              (login page)

---

CSS KEY STYLES:
---------------
- Sidebar: transform: translateX(-220px) when collapsed
- Sidebar transition: transition: transform 0.3s ease;
- Quiz mode: #content.quiz-mode with centered flexbox
- Buttons: smooth transitions on hover, rounded corners (border-radius: 10px)

---

JAVASCRIPT FUNCTIONS NEEDED:
----------------------------
- toggleSidebar() - toggles "collapsed" class on sidebar
- toggleTheme() - switches between dark/light themes
- Quiz rendering functions in quiz-mode (centered)
